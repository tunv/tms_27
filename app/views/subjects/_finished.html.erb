<%= form_for current_user.user_subjects.find_by(subject_id: subject.id),
  remote: true do |f| %>
  <div><%= f.hidden_field :status, value: Settings.finished %></div>
  <%= f.submit t("User.training"), class: "btn" %>
<% end %>
